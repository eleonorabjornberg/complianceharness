# Golden subject

A fixture tree whose reports are committed next to it. It is deliberately
half-documented, so the golden reports carry every kind of verdict the
packs can currently produce against a tree without history.

## Purpose

Forecasts the thing.

## Evaluation

Rolling origin with a purge gap. To reproduce: `make check`.

Owner: a named human.
